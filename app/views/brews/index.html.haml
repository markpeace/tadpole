%h1 Listing brews

%table
  %tr
    %th User
    %th Name
    %th Date
    %th
    %th
    %th

  - @brews.each do |brew|
    %tr
      %td= brew.user_id
      %td= brew.name
      %td= brew.date
      %td= link_to 'Show', brew
      %td= link_to 'Edit', edit_brew_path(brew)
      %td= link_to 'Destroy', brew, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Brew', new_brew_path
